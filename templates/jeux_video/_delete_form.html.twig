<form method="post" action="{{ path('jeux_video_delete', {'uid': jeux_video.uid}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ jeux_video.uid) }}">
    <button class="btn">Delete</button>
</form>